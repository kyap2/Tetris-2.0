document.addEventListener('DOMContentLoaded', () => { //Links to html 
    const grid = document.querySelector('.grid');
    // everytime you change grid js affects html and css 

    let squares = document.querySelectorAll('.grid div');
    const ScoreDiaplay = document.querySelector("#score");
    const StartBtn = document.querySelector("#start-button");
    const width = 10;
    /* 
    Here she just went over the js, she said that the query selectors are used to grab the ID name aka the # and names from certain IDs
    in the HTML code, she made the set width to 10 as a constant value that does not change ie the tetris block stays at 10 pixels?

    */

})


